export enum AppRoutes {
  DASHBOARD = "/",
  LOGIN = "/login",
  SIGN_UP = "/signup",
  PROFILE = "/p/:username",
  NOT_FOUND = "/not-found"
}

export const DEFAULT_IMAGE_PATH = '/images/avatars/default.png';
